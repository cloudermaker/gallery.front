import { Layout } from '../component/layout';

export const NotFound = (): JSX.Element => {
  return (
    <Layout>
      <section>
        <h1>This is the 404 page</h1>
      </section>
    </Layout>
  );
};
