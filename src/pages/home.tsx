import { Layout } from '../component/layout';

export const Home = (): JSX.Element => {
  return (
    <Layout>
      <section>
        <h1>This is the home</h1>
      </section>
    </Layout>
  );
};
