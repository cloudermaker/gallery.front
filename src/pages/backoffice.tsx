import { Layout } from '../component/layout';

export const Backoffice = (): JSX.Element => {
  return (
    <Layout>
      <section>
        <h1>This is the the Backoffice</h1>
      </section>
    </Layout>
  );
};
