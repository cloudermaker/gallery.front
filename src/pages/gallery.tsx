import { Layout } from '../component/layout';

export const Gallery = (): JSX.Element => {
  return (
    <Layout>
      <section>
        <h1>This is the gallery</h1>
      </section>
    </Layout>
  );
};
