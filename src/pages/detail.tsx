import { Layout } from '../component/layout';

export const Detail = (): JSX.Element => {
  return (
    <Layout>
      <section>
        <h1>This is the detail page</h1>
      </section>
    </Layout>
  );
};
